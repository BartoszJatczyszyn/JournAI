# Example environment overrides for docker-compose
# Copy to .env (root) and adjust if needed.

DB_USER=diary_user
DB_PASSWORD=diary123
DB_NAME=diary
# DB_HOST is set via docker network service name (db)
# Optionally override port mapping
HOST_PORT_BACKEND=5002
HOST_PORT_POSTGRES=5432
