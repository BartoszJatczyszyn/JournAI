{
    "python-envs.defaultEnvManager": "ms-python.python:pyenv",
    "python-envs.pythonProjects": [],
    // Treat .importlinter as an INI/TOML-style config, not Python, to avoid Pylance parsing errors.
    "files.associations": {
        ".importlinter": "ini"
    }
}